# --- OS & Editor Files ---
.DS_Store
Thumbs.db
*.sublime-*
*.code-workspace
._*

# --- IDE/Editor Directories ---
.vscode/
.idea/

# --- Logs & Temporary Files ---
*.log  # Excludes all logs except those in /logs
!logs/  # Keep the logs directory (but ignore its contents if empty)
*.tmp
*.swp

# --- Python Artifacts ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# --- LaTeX/PDF Publishing Files ---
*.aux
*.out
*.toc
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.synctex.gz

# --- Script Outputs ---
*.pdf  # Ignore generated PDFs (use /publishing for source files)

# --- Environment/Secrets ---
.env
.env.local
.secret
credentials.json

# --- Project-Specific Exclusions ---
# Ignore ZIP archives (already tracked in root)
*.zip

# --- Dependency Directories ---
node_modules/
venv/